import React from 'react';
import './header.css';

const Header = () => {

    return (
        <header id="main-header">
                <h4 className="logo"> Nutrição</h4>
                <h4 className="message">Bem Vindo!</h4>
        </header>
    );
}

export default Header;